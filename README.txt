Run with node, entry point is app.js; or use npm start

App runs at a hard-coded http://localhost:3000/

If dependencies are missing, run:

npm install && bower install

Bing API key needs to be inserted into app.js before calls to that service will work